// Firebase configuration
// Replace these values with your Firebase project config
// Get your config from: https://console.firebase.google.com/project/_/settings/general

/*
To enable Firebase authentication:
1. Go to https://console.firebase.google.com/
2. Create a new project or select existing
3. Add a web app (</> icon)
4. Copy the firebaseConfig object
5. Replace the config below with your values
6. Enable Authentication in Firebase Console → Build → Authentication → Get Started
*/

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abc123"
};

// Note: This is a placeholder config. Replace with your actual Firebase config.
// The sign-in/sign-up pages will work with Firebase once you add your config.
